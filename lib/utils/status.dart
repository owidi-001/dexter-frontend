enum AuthenticationStatus {
  unknown,
  authenticating,
  authenticated,
  unAuthenticated
}

enum LoadingStatus{
  unknown,
  loading,
  loadingSuccess,
  loadingFailure
}

enum MessageTypes { success, failure, informative, warning }
